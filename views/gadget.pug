extends layout

block content
    h1 This is Gadgets. page
    p Welcome to Pug #{title}
    - const results = [ { name: 'mobiles', quantity : 'one', price: 20 }, { name: 'refrigirators', quantity: 'three', price: 30 }, { name: 'Laptops', quantity: 'two', price: 15 }]

    div.container-fluid
    div.row
        each item,index in results
            div.col-sm-2
            div
            div(class="col-sm-2" onclick=`buy (${index})`) Name: #{item.name}
            div(class="col-sm-2" onclick=`buy (${index})`) Size: #{item.quantity}
            div(class="col-sm-2" onclick=`buy (${index})`) Price: $#{item.price}
    script.
      function buy(itemNum){
        alert(`you purchased item ${itemNum+1}`)
     }

       
        